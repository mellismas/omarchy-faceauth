Recorded 2026-09-22 while the user was on a phone call: talking, phone in
hand, attention elsewhere, with the daemon in record-only mode (each window
runs its full 14 s). The item labels say what each window asked for; the
gesture windows may or may not contain a deliberate gesture, so this corpus
sets no floors. Its rule is the safety one: no non-gesture window may ever
produce a gesture, and no nod window may produce a shake or the reverse.
The 21st file is the root window that restored the config afterwards.
